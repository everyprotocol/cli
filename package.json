{
  "name": "every-cli",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build 'src/**/*.ts' --outdir dist",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "commander": "^13.1.0"
  }
}
